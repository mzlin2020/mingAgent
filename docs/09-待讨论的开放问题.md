# 09 · 待讨论的开放问题

> 这些是需要在对应能力开工前拍板的决策点。每条给出选项、倾向和理由；讨论定案后写入
> `adr/`，并更新对应文档。**状态最后核对于 2026-08-13：M2 已完成；已被后续 ADR 取代的
> 长段落作为决策历史保留，但标题和当前结论必须明确标注。**

---

## ✅ 已定案（2026-08-04）

| 问题 | 结论 | ADR |
|---|---|---|
| A1 技术栈 | Electron + TypeScript **7.0**（tsgo）单语言 monorepo | [0001](./adr/0001-桌面技术栈选型.md) |
| B1 Agent 循环 | 单主循环 + `todo` 工具，子 Agent 是能力不是阶段 | [0002](./adr/0002-Agent循环形态.md) |
| C1 默认权限 | 已由 ADR-0039 收敛为 `allow | deny`：无档位、无审批；红线不可放开 | [0039](./adr/0039-放弃审批模式.md) |
| E1 主界面 | 对话为主 + 可展开观察面板，不内置编辑器 | [0004](./adr/0004-主界面形态.md) |
| E3 多会话呈现 | 去侧栏；顶栏 tabs + Home（新建在 Home）；汉堡仅设置/帮助；UI 仍单焦点 | [0037](./adr/0037-桌面端壳层导航与会话入口.md) |
| B2 工具并发粒度 | 混合式资源声明，声明不了的降级 exclusive | [0005](./adr/0005-工具并发与资源声明.md) |
| B3 上下文压缩 | 分层混合，75% 阈值，主模型压缩一次并持久复用；M2-h 已实现 | [0048](./adr/0048-M2-h上下文预算与持久摘要.md) |
| A2 CLI 形态 | **要做，但延后到 M4**；架构约束（runtime 不依赖 Electron）M0 起 CI 强制 | [0007](./adr/0007-平台支持分级.md) 相关 |
| 平台支持 | Linux 为 Tier 2（除计算机操作外全功能） | [0007](./adr/0007-平台支持分级.md) |
| 事件持久化边界 | 持久/瞬态分层 + upcaster 演进 + trace 分离；子 Agent 独立 seq 空间 | [0008](./adr/0008-事件持久化分层与演进.md) |
| 工具 schema 与结果 | 可序列化子集 + 注册时断言 + 运行时统一截断 | [0009](./adr/0009-工具Schema子集与结果截断.md) |
| 契约实现级规格 | 全部收敛到 [10-契约设计](./10-契约设计.md)，M0 可直接照写 | 0008 / 0009 |
| 双编译器工具链 | TS 7 编译 + TS 6 供 API，别名双装 | [0010](./adr/0010-TypeScript双编译器工具链.md) |
| 契约实现期偏离 | 补白 4 处、修正 3 处、新坑 4 处；护栏必须经违规演练才算交付 | [0011](./adr/0011-契约与内核实现期的偏离.md) |
| 地基复审 | 安全边界 4 处失效已修；安全规则必须按"运行时真会传的字符串"测 | [0012](./adr/0012-地基复审与安全边界修正.md) |

**由 A1 派生的工具链冒烟验证已完成**：TS 7 编译 + TS 6 API 的双装装配由
`scripts/assert-toolchain.mjs` 与 CI 持续检查。

---

## A. 形态与技术栈

### ~~A1. 桌面外壳：Electron 还是 Tauri？~~ ✅ 已定案 → [ADR-0001](./adr/0001-桌面技术栈选型.md)

Electron + TypeScript 7.0。内核对 Electron 零依赖（CI 强制），保留未来换壳退路。

### ~~A2. 要不要保留 headless / CLI 形态？~~ ✅ 已定案

**要做，但延后**：优先客户端形态，CLI 作为产品排到 **M4**。

但**架构约束从 M0 就生效**：`@xm/runtime` 不得依赖 Electron，由 dependency-cruiser 在 CI 强制；M0 提供一个 headless 冒烟测试（无 GUI 跑通一轮完整对话）作为持续检验。

> 约束是免费的、且必须从第一天有；产品形态可以等。两者分开，就拿到了扩展性检验的全部价值而不付产品化成本。
>
> 另外：CLI + Linux Tier 2 组合起来，未来可以把小明跑在服务器上——这条路径现在不用走，但接口不要堵死。

### ~~A3. 是否复用 manusAgent 的 Python 代码？~~ ✅ 已由实现定案

- **结论：不复用，全新 TS 实现。** M0–M2 已按 TypeScript 单语言 monorepo 交付；Python 未来只可能作为**可选插件运行时**（数据分析、本地推理场景）。

---

## B. Agent 循环

### ~~B1. 单主循环 vs Planner/Executor 双 Agent？~~ ✅ 已定案 → [ADR-0002](./adr/0002-Agent循环形态.md)

单主循环 + `todo` 工具（清单在 UI 渲染成进度视图），子 Agent 是按需派生的能力而非流程阶段。

### ~~B2. 并行工具调用的默认策略？~~ ✅ 已定案 → [ADR-0005](./adr/0005-工具并发与资源声明.md)

混合式：`Tool.resources?(input)` 可选声明，能声明的做路径级冲突检测，声明不了的降级 exclusive。

### ~~B3. 上下文压缩策略？~~ ✅ 已定案 → [ADR-0006](./adr/0006-上下文压缩策略.md)

分层混合，75% 触发，主模型压缩且只压一次并缓存。**派生约束**：`Tool.available()` 的动态过滤结果不得进入稳定前缀，否则 prompt cache 全失效。

---

## C. 安全与权限

### ~~C1. 默认严格度？~~ ⚫ ADR-0003 的档位答案已被 [ADR-0039](./adr/0039-放弃审批模式.md) 取代

现行结论是：判定只有 `allow | deny`，无规则命中即放行；红线跨层最先判，内置拒绝清单和
用户规则继续生效。写入/删除工具的 checkpoint v2 已在 M2-c 成为执行前关键路径。

### C2. 本地执行是否强制沙箱？ 🟡 部分定案

- 强制 sandbox-exec / Job Object 会带来兼容性问题（某些工具链在沙箱内行为异常）。
- **倾向**：默认不强制（依赖策略闸门 + 还原点），但提供"沙箱模式"开关，且执行来源不明的代码时**自动**切到容器执行器。
- **2026-08-07 进度**：`shell.exec` 落地时（[ADR-0026](./adr/0026-命令的主张分解与argv契约.md)）把
  "默认不强制"这一半坐实为几条具体、可测的进程级约束——env 白名单（不是黑名单）、
  stdin 接 `/dev/null`、超时 + 按进程组 kill、输出上限。**"沙箱模式"开关与
  "不可信来源代码自动切容器执行器"两样倾向里写的东西仍未实现**，C2 因此还不能
  整体转 ✅。真正的隔离仍然靠策略闸门（命令拆出的主张撞路径 / 主机红线）兜底，
  不是进程沙箱。

### ~~C3. 提示词注入的权限降级机制会不会太吵？~~ ⚫ 降级机制已被 [ADR-0039](./adr/0039-放弃审批模式.md) 删除

只针对不可撤销的能力子集：网络外发、文件删除、`git push`、GUI 输入注入。全局收紧会误触发到被用户关掉，等于不存在。

**2026-08-10 修正**：上面这个答案不完整，而缺的那一半在真实体验里当场暴露——用户让小明联网搜索，每个网址都要授权一次，开到「帮我批准」/「完全访问权限」也照问不误。

原因不是"降级范围太宽"，而是 **`net.fetch` 同时是污点源和降级对象——它自己污染自己**：第一次 `web.fetch` 就置上 `untrustedContext`（粘性），此后每一次 fetch 都被降级打回。而降级排在 `evaluate()` 最后一步，也就排在 YOLO 之后、**所有层之后**，于是连用户当场点的「本会话都允许」也一并被压掉——同一个域名、同一个能力、同一个污染源，被反复问到用户不再看内容为止。

限定"只作用于不可撤销子集"防的是**跨能力的误伤**（读个网页就不能写文件了），它防不了**同一个能力在自己造成的污染下被无限重复询问**。ADR-0034 补上后者：满足「session 层 + 带具体 target + 授权晚于污染」三个条件的 allow 穿透降级。仍未解决的是"一次搜索仍然是每个新域名一个框"（根因是没有 `web.search` 工具），见 ADR-0034 的后果一节。

### ~~C4. `PermissionRequest.target` 是一个字符串，够用吗？~~ ✅ 已定案（2026-08-05）→ [ADR-0020](./adr/0020-非路径target的规范化契约.md)

**结论：每个能力恰好一种 target 语义（`path` / `host` / `command` / `opaque`），穷尽映射，没有契约的那一种失败关闭。**

- `host`（`net.fetch` / `browser.control`）完整落地：大小写、默认端口、FQDN 尾点、userinfo 归一；数字进制 IP、百分号编码、非 ASCII、非 http(s) 一律拒绝。
- `command`（`shell.exec` / `process.spawn`）**契约已于 2026-08-07 落地** → [ADR-0026](./adr/0026-命令的主张分解与argv契约.md)：结构化 `{ argv, cwd }` 归一到规范形式，但归一本身不是防线；真正拦截靠把命令拆成一组「能力 + 目标」的主张，交给已有的路径 / 主机规则去判（不是"执行器沙箱"，C2 那句话被修正——见 C2 的 2026-08-07 进度）。含 `$(...)` / 反引号 / `$VAR` / 通配符等展开结果依赖运行时环境的构造一律判不了直接拒。红线仍不许建立在 command target 上，普通规则只能作便利过滤。
- `opaque` 明确**不是安全边界**，红线不得建立在它上面（构造期断言）。

原倾向里那句"不要试图用 glob 匹配命令行"被完整采纳，并且比原计划更进一步：不是"匹配得弱一点"，而是**在契约落地之前一条也不许写**——顶着的那个 glob 会让所有人以为这里已经防住了。

### ~~C5. YOLO 是否应该越过**用户自己写的** deny 规则？~~ ⚫ YOLO 随 [ADR-0039](./adr/0039-放弃审批模式.md) 删除

**历史结论：不越过。** 现行系统没有 YOLO 或 `ask`；仍保留下来的约束是“同层 `deny` 胜
`allow`，immutable 红线不能被任何层覆盖”。

拍板前实测确认了问题确实存在：用户写 `deny fs.delete /home/ming/work/prod/**`，balanced 档 DENY、yolo 档 ALLOW（`ruleId` 变成 `builtin.tier-fallback`）。

理由按原倾向：用户写下一条 deny 是**长期意图**，开 YOLO 是**"这一小时别烦我"**——前者省的是确认框，后者删掉的是用户唯一能表达"这里绝对不行"的手段。而且它恰好在最危险的时候失效，因为开 YOLO 正是为了放手让它长时间自己跑。

实现是把 `engine.ts` 里 YOLO 分支挪到普通 deny 之后（求值顺序改为 `红线 → deny → YOLO → ask → allow → 档位兜底`）。docs/06 §3 "YOLO = 全 allow，只有绝对红线不受影响"的措辞需要同步改成"只有 deny 不受影响"。

### C6. 桌面端如何降噪：「替我审批」与「完全访问权限」两种模式 ⚫ 已被 [ADR-0039](./adr/0039-放弃审批模式.md) 整体取代（2026-08-07 提出 → 2026-08-08 ADR-0030 → 2026-08-11 作废）

> **⚫ 2026-08-11：这条问题本身被取消了。** 用户的决定是**彻底放弃审批**——不是把"要不要问"
> 调得更聪明，而是不再问。判定收敛成 `allow`/`deny`，三档开关、注入降级、会话授权、
> 审批 UI 全部删除（[ADR-0039](./adr/0039-放弃审批模式.md)）。
>
> 下面的记录**原样保留**，因为它是一条完整的、值得读的失败路径：C6 提出的两个模式各自
> 落地（ADR-0030），然后连着三次真实反馈、三份修补 ADR（0034/0035/0036），
> 最后结论是这条路走到头也不对——**噪音的根不在某一条路径上，在"要不要问人"这个动作本身**。
> 那句"这一步不该被一个 UI 开关的命名顺带做掉，需要单独拍板"仍然成立，
> 只是拍板的结果与当时设想的相反。


**来源**：用户真实体验反馈——用小明做一个"写个 todolist"这么朴素的任务，
中途弹出的确认框超过 8 次，用户全程只在点"允许"。M1-c 的审批 UI（本次 /
本会话 / 永久 / 拒绝）解决的是"每一条主张要不要问"，没有解决"这一整段
时间我根本不想被问"——两者是不同层面的问题，前者按*目标*降噪
（同一个文件第二次不再问），后者要按*时间窗口*降噪（接下来这一段随便跑）。

用户提的诉求是两种模式，字面意思不完全等价，需要在设计时把它们拆清楚：

1. **「替我审批」**——听起来是"这一段时间的 `ask` 我都替你点允许"。这与
   已经存在、但从未在桌面 UI 上暴露过的 `PermissionTier.yolo` 高度重合：
   `engine.ts` 的 YOLO 语义是"跳过 `ask`，不跳过任何 `deny`"（C5 定案，
   红线与用户自己写的 deny 规则原样生效）。如果这就是用户想要的东西，
   缺的只是**桌面端一个能切换它的开关**——目前 `tier` 只能在 `config.json`
   里手改，改完还要重启会话，普通用户不会去碰配置文件。开关放在哪个
   范围（单次回合 / 整个会话 / 全局默认）是这里要定的第一件事。

2. **「完全访问权限（允许任何操作）」**——字面意思比 YOLO 更进一步：
   **允许任何操作**，那就不只是跳过 `ask`，是把 `deny` 也跳过去。这与
   本项目从 ADR-0003 起就有的地基性质直接冲突——C1 定案写的是
   **"红线不受档位影响"**，`engine.ts` 里红线在所有档位、所有层之前
   全局最先判，没有任何一条分支能绕开它。如果真要做一个字面意义上
   "允许任何操作"的模式，等于要新开一个凌驾于红线之上的层级，这不是
   在现有 `PermissionTier` 上加一档就能实现的，而是要重新回答
   "红线还是不是红线"这个已经定案过的问题——**这一步不该被一个 UI
   开关的命名顺带做掉，需要单独拍板**，包括但不限于：
     - 这个模式是否连 `red.fs-delete-home-root` / `red.self-modify-*`
       这类红线也放行？如果放行，"红线"这个词在文档里就名不副实了；
       如果不放行，那它本质上还是 YOLO，只是文案上写成了"完全"，
       需要在 UI 文案上讲清楚"完全"指的是"不再确认"而不是"没有底线"，
       避免用户以为开了这个开关就真的没有任何保护。
     - 即使真的要做一个能越过红线的档位，至少要有比"点一下按钮"更高的
       操作成本（例如要求先手改配置文件、或者要有比普通确认框更醒目的
       二次警告），否则和"这个应用没有权限模型"没有实质区别。

**倾向（未定案）**：先做「替我审批」——把已经落地、已经过 C5/ADR-0017
验证过安全语义的 YOLO 档暴露成桌面端的一个开关（大概率是会话级，跟着
`SessionState` 走，不动全局 `config.json`），这一步风险可控、实现量也小。
「完全访问权限」按上面列的问题单独讨论，不与前者同批做，避免"顺手就把
红线绕过去了"这种事在一次不起眼的 UI 需求里发生。

**2026-08-08 部分定案 → [ADR-0030](./adr/0030-桌面端三档审批模式.md)**：
「替我审批」按倾向落地，桌面端新增会话级、不持久化的 `ApprovalMode` 三档
UI（请求批准/帮我批准/完全访问权限），`帮我批准` 就是暴露出来的 YOLO 档
（用户确认过要放开到连 `shell.exec` 也不再问）。**「完全访问权限」没有
按字面意思实现**——它与「帮我批准」复用同一套判定机制（都映射到 `yolo`，
不新增、不修改 `PermissionTier`，不碰 `evaluate()`），区别只在桌面 UI 的
开启门槛（需要二次确认）与文案。上面列的两个问题——"是否连红线也放行"、
"需不需要比点按钮更高的操作成本"——**仍未解决**：本轮的答案是"不放行"，
第二个问题靠一次内联二次确认部分满足，但都是刻意搁置而不是拍板，留给
将来要做"字面意义上、真的能越过红线"的模式时单独开 ADR。

**2026-08-10 第二个真实实例 → [ADR-0034](./adr/0034-知情授权穿透注入降级.md)**：
用户开着「帮我批准」/「完全访问权限」联网搜索，每个网址仍被要一次授权。
这次的噪音**不是 ADR-0030 没做完**，而是判定顺序上的一个洞：注入降级排在
`evaluate()` 第 5 步，YOLO 在第 4 步——两档审批模式都映射到 `yolo`，
于是第 4 步刚把 `ask` 变成 `allow`，第 5 步又变回 `ask`，
**按设计就穿不过去**。同一个原因让会话授权层与用户配置层的 allow 一并失效。

这条记录在这里是因为它说明 C6 那句"按*目标*降噪 vs 按*时间窗口*降噪是两个
不同层面的问题"还漏了第三个层面：**两种降噪手段之间会互相抵消**。
ADR-0030 做的是时间窗口降噪，M1-c 做的是目标降噪，而注入降级把两者同时压掉了，
三者各自都对，合起来是"什么都没降"。

**2026-08-10 第三个真实实例 → [ADR-0035](./adr/0035-注入降级感知审批档位.md)
+ [ADR-0036](./adr/0036-只检查的主张.md)**：同一天，ADR-0034 落地之后用户再次反馈
「搜一个今日新闻点了 10+ 次允许」。ADR-0034 消掉的是"**同一个**域名被反复问"，
而一次搜索里**每个域名都是新的**——一条都不在它的覆盖范围内，它自己的「后果」段
就写着这条残留，只是当时没意识到那等于"用户的症状一点没变"。

**教训（值得单独记）：修完一个降噪问题，要按用户的原始场景数一遍框，
不要按"我修掉的那条路径"数。** ADR-0034 的端到端用例断言的是"3 条降到 1 条"，
而用户的场景是 11 个不同域名——用例绿了，症状还在。ADR-0035 的用例因此
直接照抄用户那句话：SERP + 5 个各不相同的域名，断言 `permission.request` **为 0**。

同一次测量还查出第二个乘数（ADR-0036）：网关为 SSRF 判定产出的"解析出的 IP"主张
也命中了 `def.net-fetch` 的 ask，于是**每次联网本来就弹两个框**。
两个乘数相乘才凑出"10+ 次"——这也说明第三个层面还得再补一句：
**噪音是会相乘的，只修掉其中一个因子，用户感觉不到差别。**

**2026-08-11 收尾 → [ADR-0039](./adr/0039-放弃审批模式.md)**：用户在四次修补之后给出了
产品层面的答案——**审批彻底不要了**。最该记住的一条：连着三份 ADR 都在回答"这一次该不该问"，
没有人回退一步问"该不该有'问'这件事"。**修一个机制修到第三遍还在同一个形状上，
就该怀疑这个机制本身，而不是继续调它的参数。**

### C7. `shell.session`（PTY）落地前必须先拍板的六个开放问题 🟢 已定案（2026-08-08 提出 → 同日 [ADR-0031](./adr/0031-shell-session-pty.md) 定案并落地最小可用版本）

**来源**：M1-d 剩余交付项排优先级时的探查——`web.fetch` 的 IP 级 SSRF 判定
（[ADR-0028](./adr/0028-web.fetch的IP级SSRF判定与DNS重绑定防护.md)）与多模态图片
（设计已存档于 [docs/08 M1-d](./08-路线图与里程碑.md)）都不碰权限判定循环与工具
执行生命周期这两处核心机制，可以照既有模式直接落地；PTY 不一样——它会正面改动
这两处刚刚才稳定下来的机制，直接写代码大概率会做出一条绕开现有不变量的旁路
（本项目历史上"规则存在 ≠ 规则生效"反复出现的形状，只是这次会变种成"架构不变量
存在，但 PTY 这条路径没有被它约束"）。以下六项必须先在一份专门的 ADR 里定案，
不是实现细节可以边写边改的那种问题：

1. **工具执行生命周期要不要引入"跨 turn 常驻会话"这个新抽象，长什么样。**
   现状：`ToolContext` 由 `dispatchCall` 每次调用现造，工具执行绑死在一次
   `dispatchCall`→`executeCall` 内（`turn.ts`）。PTY 会话本质是"打开一次、跨越
   多个 turn 持续存在、被后续多次调用共享同一句柄"的对象，现有抽象里没有这种
   模式。要定：会话句柄放在哪一层持有（`SessionRuntime`？`ToolRegistry`？一个新的
   `SessionManager` 端口？）；打开/写入/读取输出/关闭是拆成四个工具还是一个工具
   带 `action` 字段（直接决定权限判定挂在哪个粒度上）；会话数量上限、空闲超时、
   异常退出时事件流该记成什么。

2. **命令级判权 vs 会话级判权——PTY 里到底判什么。**
   `command-claims.ts` 的 `analyzeArgv()` 假设"一次调用 = 一条完整命令行"，是
   无状态纯函数。PTY 场景是用户/模型往一个已经打开的 shell 里逐字符/逐行敲，
   "这一批字节构成了一条完整命令"本身就很难界定（可能没敲完就等一会儿、或者一次
   粘贴进去三条用分号分隔的命令）。要定：打开会话时做一次粗粒度 ask（此后不再
   逐条判权，靠回显+能随时中断兜底），还是坚持每条完整命令重新走一遍
   `analyzeArgv`+claim+policy（如果坚持，怎么从连续字节流里可靠切出"一条命令"是
   一个 shell 语义问题，不是简单字符串处理能解决的）——两个选项的安全含义完全
   不同，必须先定，不能写代码时临时决定。

3. **`SessionState.pendingPermission` 从单槽改队列的具体设计。**
   现状是单槽（`PermissionRequest | undefined`），上一轮"多 claim 排队 bug"
   （见 [ADR-0026 补记](./adr/0026-命令的主张分解与argv契约.md)）的修法前提是
   "一次 `dispatchCall` 内的多个 claim 是提前算好、按顺序遍历的有限集合，
   `turn.ts` 完全掌控发问节奏，任意时刻只有一个在等答案"。PTY 场景可能出现真正
   并发的多个 ask（比如同时有两个 PTY 会话都在等审批），这个前提不成立。要定：
   `pendingPermission` 改成 `Map<requestId, PermissionRequest>` 还是队列，
   `reduce.ts` 怎么处理"新请求到达时上一个还没答完"；桌面端 `PermissionCard`
   只渲染一张卡片，改成渲染多张后 UI 上"先答哪个"要不要有顺序保证；
   `store.ts` 的 `respondPermission` 按 requestId 匹配的逻辑大概率不用改，但要
   在 ADR 里明确写清楚"为什么不用改"，避免有人顺手又加一层不必要的匹配逻辑。

4. **`node-pty` 的 N-API 现状要先核实清楚，不是假设。**
   [ADR-0016](./adr/0016-原生模块与打包.md) 明确点名 `node-pty` 是"两轨 ABI
   问题会复活"的触发条件——现役唯一原生依赖 `better-sqlite3` 是纯 N-API、随包发
   prebuild，`electron-builder.yml` 配了 `npmRebuild: false`。`node-pty` 历史上
   是否已经全面 N-API 化，要实际去查它当前发布版本的 binding.gyp/prebuild 策略，
   不能凭印象往下走。要定：如果仍不是纯 N-API，是恢复 `electron-rebuild`（连带
   新增校验产出 `.node` 文件的 CI 检查），还是找替代实现，还是先只支持类 Unix、
   Windows 上暂不做；打包三平台的风险清单（`asarUnpack`/`files`/交叉编译）要不要
   重新过一遍。

5. **终端输出的 IPC 推流方式——要不要换掉现有的 `EventBus`→`webContents.send`。**
   [ADR-0015](./adr/0015-进程与IPC边界.md) 明写现有 IPC 推流"没有背压"，且把
   "实时终端输出"列为需要重新评估、可能要换 MessagePort 直连的场景。终端输出的
   密度（用户在里面跑一个日志密集的命令，或者 `top`/`vim` 这类全屏刷新的程序）
   远超目前唯一验证过的场景（`message.delta` 的 token 级流）。要定：继续复用
   现有机制（意味着接受某种程度的丢帧/延迟风险，需要明确能接受到什么程度），
   还是引入 MessagePort（意味着这次范围要连带做 ADR-0015 遗留的这块重新评估，
   工作量会超出"只做 PTY"）——这个决定必须先做，它决定 PTY 的实现要不要顺带动
   IPC 基础设施这条更底层的路。

6. **UI 侧的终端渲染选型。**
   现状零 xterm.js 依赖、零 ANSI 处理、无持续追加型 console 组件；唯一可复用的是
   `App.tsx` 里的 `stickToBottom` 自动滚动逻辑。这项不算必须先拍板才能动工的架构
   决策（选型基本没有别的合理选项），但要在 ADR 里至少写一句选型结论 + 大致的
   组件边界（终端视图是不是要走 IPC 之外的直接渲染路径），避免和问题 5 的推流
   方式决策脱节。

**2026-08-08 定案 → [ADR-0031](./adr/0031-shell-session-pty.md)**：六项逐一定案，
核心结论——四个独立工具（open/write/resize/close），`PtySessionManager` 全应用
共享一份、按 xm 会话分区（与 C6 的 `ApprovalModeStore` 同一形状）；**打开会话时
问一次、之后完全不再判权**（`write`/`resize`/`close` 声明空能力集，不是"依赖
用户选 session 作用域"）——代价是红线/deny 对会话内容零覆盖，这一点比 C6 的
"帮我批准"更彻底（那里红线仍逐条判，这里从一开始就没有 claim），必须在 ask
文案里讲清楚；`pendingPermission` 不用改队列（实测 `dispatchCall` 目前是顺序
执行，不产生并发 ask）；`node-pty` 已核实是纯 N-API、自带 prebuild（但只覆盖
Windows/macOS，Linux 需要本地编译），在本项目钉住的 Electron 43 上实测通过；
IPC 复用现有 `EventBus` 推流，新增的三个事件类型不需要任何新 IPC 代码；UI
选型 `@xterm/xterm`，`TerminalPanel` 是写死组件、v1 只读（人不能直接在里面
打字）。已实现并测试，非本轮范围的邻近问题（Windows/macOS 未验证、
`EventBus` 无背压、通用面板注册表未实现、人类接管输入未做）记在 ADR-0031
的"遗留"一节，**逐条转成本文档 C8 的完善计划**，推到后续阶段再排期。

---

### C8. `shell.session`（PTY）的完善计划 🟡 原始 stdin 方案已关闭，余项待真实需求

> **当前处置（2026-08-13）**：原始 stdin 路线已由 [ADR-0040](./adr/0040-M1.5自主安全边界与受控终端.md)
> 关闭，改为受控 argv + 只读状态；目录/大文件 checkpoint 已由 M2-c 完成。完整 PTY 输出归档、
> REPL/全屏交互与 OS 沙箱仍未实现。M2-i 子 Agent 因为串行且只读，未开放任意第三方执行。

以下八项表格保留 ADR-0031 时的原始问题现场，不能当作现行 PTY 接口清单；其中关于原始输入、
单槽审批和 M2 checkpoint 的前提均已被后续 ADR 改写。

**来源**：ADR-0031 交付的是**刻意收窄范围的最小可用版本**——用户明确要求这一轮
"先跑起来"，完善工作留到后续阶段。这里把 ADR-0031"遗留"一节的七项逐一转成
可排期的条目，避免它们只活在一份 ADR 的尾巴里、后续排优先级时被忘掉。**在这些
条目动工前，v1 的行为（尤其"红线/deny 对会话内容零覆盖"这一条）保持不变、
继续按 ADR-0031 的文案如实告知用户。**

| # | 条目 | 现状 | 需要先想清楚的问题 | 建议阶段 |
|---|---|---|---|---|
| 1 | 会话内容也逐条判权 | `write`/`resize`/`close` 零 claim，红线/deny 不覆盖会话内敲的任何内容 | 怎么从连续字节流里可靠切出"一条完整命令"——C7 问题 2 已判定这不是简单字符串处理能解决的 shell 语义问题；也可能是"按回车切一次、允许误判"这类有损方案，需要单独出 ADR 权衡 | 未定，需求不明确前不排期 |
| 2 | Checkpoint 覆盖 PTY 造成的文件改动 | `checkpoint.ts` 只认 `fs.write`/`fs.delete` claim，PTY 全零 claim，会话里 `rm` 掉文件没有任何还原点 | 是在 `open` 时打一个 cwd 的基线快照，还是轮询/监听 cwd 内变化再补 checkpoint 事件；后者代价是要引入文件系统监听 | M2（本来就要做 checkpoint 与撤销的通用化） |
| 3 | `pendingPermission` 改队列 | 单槽，目前不产生并发 ask 是因为 `dispatchCall` 顺序执行 | 子 Agent（M2）落地后，并发 ask 是否会真实出现；如果出现，这项和 PTY 无关，是子 Agent 带来的通用问题，PTY 只是提前发现了这个前提可能不成立 | 与 M2 子 Agent 并发模型一起设计，不单独为 PTY 做 |
| 4 | Windows/macOS 上 `node-pty` 实测 | 只验证了 Linux；npm 包的 prebuild 只覆盖 win/mac，Linux 反而是本地编译的那一个 | 三平台 CI 的 `desktop` job 需要远端仓库才能跑（M0-b 记的同一笔账）；届时顺带把 PTY 的 spawn/写入/关闭跑一遍 | 有远端仓库、CI `desktop` job 跑起来之后 |
| 5 | `EventBus` 推流背压 / MessagePort 直连 | 高频输出（`top`、日志洪水）可能丢帧或有延迟，本轮接受这个已知限制 | 是否真的出现用户能感知的丢帧（需要先有真实使用反馈），以及这项工作本质是 ADR-0015 的独立遗留项，不该被 PTY 一个功能牵头做 | 有真实丢帧反馈之后，与 ADR-0015 遗留一起处理，不单独为 PTY 提前做 |
| 6 | `shell.session.closed` 的 `tail` 补 `tailRef` | 只有定长环形缓冲，超限丢弃更早内容，没有 `shell-exec.ts` 那套 blob 落盘 | 按 ADR-0008 的演进规则新增可选字段不需要动 `version`，实现成本不高，主要是判断"完整无损回放"这个需求有没有真实出现 | 低优先级，出现真实需要时再加 |
| 7 | 人类接管终端输入 | v1 `disableStdin: true`，只有模型能写，人只能看 | 接管后的输入该不该经过判权（红线在这条路径上本来就不覆盖，人类直接打字是不是应该豁免，还是同样受限）；卡在交互式 `sudo` 密码提示这类场景要不要专门处理——这是一个新的、C7 六问没覆盖到的开放问题，需要先单独讨论再动工 | 需要先讨论清楚语义，不能直接实现 |
| 8 | 通用观察面板/渲染器注册表 | `TerminalPanel` 是写死组件，不是 ADR-0004/docs-05 规划的插件化面板系统的落地 | 通用系统本身还没做出来，是独立的、更大的工程；`TerminalPanel` 要不要迁移进去取决于那套系统怎么设计 | 那套面板/渲染器注册表本身启动之后，作为它的第一个真实迁移案例 |

**排期原则**：不因为"顺手"提前做——第 3、5 条明确写着"这本质是别的模块的遗留项，
不该被 PTY 一个功能带头做"；第 1、7 条是需要先回答清楚语义问题、不能直接写代码
的那一类（与 C7 本身"先拍板再动工"的教训一致）；第 4 条卡在"需要远端仓库"这个
本项目从 M0-b 就欠着的环境前提上，不是 PTY 独有的债。

### C9. 红线与拒绝清单的展示与用户配置 🟡 只读展示已完成，UI 编辑待定

**来源**：用户在决定放弃审批时同时给出的下一步——"只给几条红线即可，**红线后续可自行
展示和配置**"（[ADR-0039](./adr/0039-放弃审批模式.md)）。

审批删掉之后，`builtinRules()` 变成一张**纯拒绝清单**（红线 + 内置 deny），
这正好是最容易渲染的形状：一条规则 = 一个 id + 能力 + target 模式 + 一句人话理由 +
是不是 immutable。用户侧唯一的权限入口是 `config.json` 的 `permission.rules`，
M1.5 已在“设置”中提供只读安全面板，展示边界、红线类别、工具可用性、PTY 模式、
密钥后端与配置问题；无人调用的永久规则写入口已删除。

要定的两件事：

仍待后续定案的是“是否允许用户从 UI 编辑规则”。如果开放，核心约束不变：

1. **⚠️ 能编辑规则的是人，不是小明。**
   `red.self-modify-*` 已经把 `packages/kernel/src/policy/**` 与
   `packages/contracts/src/permission/**` 挡住了。但如果规则改成"从 UI 写入某个数据文件"，
   **那个文件必须同样进 `SELF_MODIFY_PROTECTED`**，否则等于给小明开了一条改红线的合法路径：
   它有 `fs.write`，而那个文件不在保护清单里。
   这与 ADR-0019「解除标记只有人能按」是同一条纪律的同一个形状——
   **能被下游写出来的东西，只许收紧，不许放松。**

顺带一条更小的：主界面刻意不常驻"当前是自动放行"的提示（用户明确要求不展示），
docs/06 那条"必须显式开启，有明确警告"已按 ADR-0039 改写成"默认开启 + 文档如实告知"。
若将来觉得需要一句一次性的告知，最合适的位置是 Home 页而不是对话区。

---

## D. 数据与存储

### D1. 会话数据是否加密？ 🟡

- 会话里会有代码、路径、可能的敏感信息。
- **倾向**：SQLite 默认不加密（性能与可调试性），但提供可选的 SQLCipher；审计库与密钥始终受 OS 级保护。
- **截至 M2 的现状（2026-08-13）**：会话库仍不加密，落在平台数据目录并依赖 OS 文件权限。
  SQLCipher 会改变当前 better-sqlite3 的预编译与发布策略，真要做时必须重算 ADR-0016 的成本。

### D2. 是否做多设备同步？ 🟢 延后

- **倾向：M7 之后再考虑**。同步会引入冲突解决、端到端加密、云依赖，与"零外部依赖"原则冲突。

### ~~D3. 索引策略？~~ ✅ 已由 [ADR-0047](./adr/0047-M2-g可重建WASM符号与FTS索引.md) 定案并实现

- **结论**：可重建的 tree-sitter WASM 符号 + FTS5 派生索引，按需增量维护；冷启动、损坏、
  短查询或未就绪时自动降级为 M2-b 的 ripgrep 文本搜索。

---

## E. 交互与 UI

### ~~E1. 主界面形态？~~ ✅ 已定案 → [ADR-0004](./adr/0004-主界面形态.md)

对话为主 + 四类可展开观察面板（终端 / 浏览器 / diff / 屏幕），面板经渲染器注册表接入，插件可贡献自己的面板。

### ~~E2. 是否内置代码编辑器？~~ ✅ 随 E1 定案

不内置。只做只读查看 + diff 审阅（逐块接受/拒绝）。这是整个项目省掉的最大一笔复杂度。

### ~~E3. 多会话并行的呈现？~~ ✅ 已定案（2026-08-10）→ [ADR-0037](./adr/0037-桌面端壳层导航与会话入口.md)

**结论**：去掉常驻侧栏；顶栏 **会话 tabs**（打开集合）+ **Home**（按 `updatedAt` 的最近会话列表，
并承载新建 / 从目录创建）；汉堡菜单仅 **设置 / 帮助** 占位（不做完整系统菜单镜像）。
主题色仍 Claude Code 暖色，布局参考 openCode。

补充边界（与实现无关的既有事实，写入 ADR 以免误解「并行」）：

- 主进程本就可以多会话 runtime 并存、后台 turn 可继续跑；
- 渲染层仍只有一个 `currentId`（单焦点），非焦点事件 UI 可忽略——tabs 表达的是打开集合，不是一窗多视口；
- 真「同时盯两个任务」仍留给 **M5 多窗口**；
- Home **暂不**展示 cwd / 项目列；关 tab **默认不删除**会话（删除/归档未立项）；
- 顶栏**无** `+`（与 Home 新建入口去冗余，见 [ADR-0037](./adr/0037-桌面端壳层导航与会话入口.md) 补记）。

状态：ADR 为 🟢 Accepted（2026-08-10 壳层已落地）。

---

## F. 自我迭代

### F1. L4（内核自修改）到底做不做？ 🟡

- 这是最有野心也最危险的一级。
- **倾向：做，但严格分级 + 应用外 diff 审阅/合并 + 不可改的红线清单**。见 [07 文档](./07-自我迭代能力.md)第 4–5 节。
- **需要你确认**：你对"它改自己的代码"的接受度到哪一步——自动提 PR 等你审？还是连提案都要你先批？

### F2. 评测集从哪来？ 🟡

- 手工构造成本高，全靠真实 trace 又有隐私与偏置问题。
- **倾向**：以真实失败 case 固化为主（每修一个 bug 加一个用例），辅以少量手工构造的核心能力用例。

---

## G. 地基复审二（2026-08-05）开出的问题

来源：[ADR-0017](./adr/0017-地基复审二-注入防御与红线能力错位.md)。前三条是那次复审**修完之后剩下的缺口**，后两条是实测出的规模问题。

**2026-08-05 进度**：G1 / G3 / G6 是 M1-a 的三个前置，已全部做完（ADR-0019 / 0020 / 0021）。剩下 G2 升为 🔴——G1 落地之后，它是注入防御上**唯一**剩下的绕过路径。

**2026-08-09 进度**：G4 / G5 提前在 M1-d.5 修完（不是原计划的 M2），见下方两条与 [ADR-0032](./adr/0032-地基复审三-M1收尾前的原则符合度审查.md)——地基复审三发现它们早就实测过却从未排进优先级表，直接证伪了 docs/01 的质量标线，因此没有再等到 M2。

### ~~G1. 不可信标记的解除入口~~ ✅ 已做（2026-08-05）→ [ADR-0019](./adr/0019-不可信标记的解除入口.md)

新增持久化事件 `trust.cleared` + `SessionRuntime.clearUntrusted()` + 渲染层常驻横幅。

三条定下来的：解除是**事件**不是内存标志；作用域是**到下一次引入外部内容为止**（`reduce` 置回 `undefined`，下一次 `tool.start` 会被 `taintOf()` 重新标上，不需要额外机制）；**只有人能按**——`ToolContext` 里没有任何记录事件的入口，工具在结构上够不着，且有用例盯着防止将来"顺手"给它加一个 `record`。

原倾向里的作用域判断被采纳。额外发现的一点写进了 ADR：解除入口自带社工面（模型可以在回复里写"请点上面那个按钮"），所以横幅必须复述事件流里的事实——哪个工具、哪个能力、什么时候。

### G2. MCP 工具与子 Agent 的污点传播 🟡 **子 Agent 已闭环，MCP 仍由闸门阻断**

现在的污点判定读的是 `tool.start` 事件里的 `capabilities`，因此覆盖不了两条路：

- **MCP 工具**若不如实声明 `net.fetch`，它拉回来的外部内容就标不出来。而 MCP server 是第三方的。
- **子 Agent（已解决）**有独立 `sessionId` 与事件流，但 M2-i 在 `subagent.end` 携带末态
  `untrustedContext`，父 reducer 按粘性语义并入。

第二条曾是完整绕过路径，现已由 [ADR-0049](./adr/0049-M2-i串行只读子Agent.md) 的真实派生链路
测试关闭；当前剩余风险只在尚未实现的 MCP 载体。

- **倾向**：MCP 工具一律**默认**按不可信内容源处理（宁可误标，不可漏标）；子 Agent 在 `subagent.end` 时把子会话的污点并回父会话。
- **2026-08-05 更新**：G1 落地之后，**这是注入防御上唯一剩下的绕过路径**。优先级从 🟡 升到 🔴。
- **2026-08-06 更新——它挡的比原先写的更早。** 路线图原话是"接 MCP 前必须做"，
  但**子 Agent 是 M2 的交付项**（M4 才是 MCP）。把"读网页"派给子 Agent，正是当前设计下
  完整的绕过路径，所以 G2 的真实位置是 **M2 之前**。
- **落地方式（2026-08-06 定）**：M1 既没有 MCP 也没有子 Agent，**没有载体就无法在真实输入下
  验证**。此时写实现，等于再造一个"测试全绿、真实输入下从未跑过"的东西——`trustLevel` 硬编码
  （ADR-0017）与 8.3 短名（ADR-0018）两次翻车都是这个形状。
  因此 **M1-e 只落一道失败关闭的闸门**（注册 MCP 工具 / 派生子 Agent 的路径上断言污点传播已实现），
  契约在 ADR 里定死，实现随载体走。手法与 ADR-0020 决策三（命令行 target）完全相同。
- **2026-08-09 更新——闸门已实现并落地** [ADR-0033](./adr/0033-G2闸门-MCP工具与子Agent的污点传播.md)：
  `ToolRegistry.register()` 拒绝 `source.kind === 'mcp'` 的工具，`SessionRuntime.record()` 拒绝
  `subagent.start`/`subagent.end`。真正的污点传播实现仍分别等 M2（子 Agent）/M4（MCP）的载体落地。
- **2026-08-13 更新——子 Agent 载体已实现** [ADR-0049](./adr/0049-M2-i串行只读子Agent.md)：
  `subagent.start/end` 闸门由真实生命周期、父子会话关系和污点回传替换；MCP 工具仍在注册时
  失败关闭，直到 M4 为“不依赖三方自报”的污点来源完成实现与反向演练。

### ~~G3. 非路径能力的 target 没有规范化契约~~ ✅ 已做（2026-08-05）→ [ADR-0020](./adr/0020-非路径target的规范化契约.md)

与 C4 同一次解决。反向演练当场打出了它的代价——去掉 host 规范化后，一条最普通的 `deny net.fetch *.evil.com` 被**八种写法全部绕过**，包括最朴素的 `https://evil.com/`（`*.evil.com` 在路径语义下压根不匹配域名自身）。

**遗留**：host 归一只是"写法"层面的防线，真正的 SSRF 防御要在发请求那一层按解析出的 IP 判定（内网段、link-local、DNS 重绑定）。M1-d 的 `web.fetch` 必须在能力网关那一层再加一道。

> ✅ **能力网关本身已于 2026-08-06 落地**（M1-c，[ADR-0024](./adr/0024-路径能力网关.md)）：
> 路径这一侧的 realpath / 相对路径 / Windows 短名全部由它解析，且**解析结果回写进入参**，
> 判定与执行因此用的是同一个字符串。此前它只是三份 ADR 里反复提到的一个名字，
> 一次都没有被创建过。`web.fetch` 的 IP 级判定接在同一个位置上。

### ~~G4. 会话回放是超线性的，且 `messages` 无上界~~ ✅ 已于 2026-08-09 修复 → [ADR-0032](./adr/0032-地基复审三-M1收尾前的原则符合度审查.md) #1

`reduce` 每条 `message.end` 都做 `messages: [...state.messages, m]`，即每条消息一次全量数组拷贝。实测（16k 条 `message.end`）：

| 事件数 | 回放耗时 | N 翻倍后的倍率 |
|---|---|---|
| 1 000 | 11.6 ms | — |
| 4 000 | 69.1 ms | ×3.79 |
| 8 000 | 110.6 ms | ×1.60 |
| 16 000 | **1121.2 ms** | ×10.14 |

打开一个用了几个月的会话要卡一秒以上，而且 `state.messages` **永不收缩**——`context.compacted` 在 reduce 里只记标记，压缩替换是 ContextBuilder 装配时做的事。对一个"长期演进"的 Agent，这是最先撞到的墙。

**2026-08-09 已修复**：`EventStore` 端口新增 `readSnapshot`/`writeSnapshot`（不变量八：纯派生、可选加速手段），`SessionRuntime` 每 500 条持久事件存一份快照，命中快照时只补读尾部。本地基准复现同一测法：n=16000 从 1077ms 降到 0.4ms（2801×），n=50000 从 22337ms 降到 1.6ms（13695×）。测试：`packages/kernel/tests/snapshot.test.ts`（往返正确性）、`packages/runtime/tests/session-runtime.test.ts`（快照触发/命中尾部回放/写失败防重入）、`packages/kernel/src/port/event-store-contract.ts`（三条端口契约用例）。回归用例固化在 `evals/regression/g4-session-replay-superlinear.json`。

### ~~G5. `readSession` 在 IPC 边界把流式读取全量物化了~~ ✅ 已于 2026-08-09 修复 → [ADR-0032](./adr/0032-地基复审三-M1收尾前的原则符合度审查.md) #1

`SqliteEventStore.read()` 特意做了 500 条一页的分页，注释写着"绝不 `all()`：一次性物化会在打开会话时占掉几十 MB 并阻塞主进程"。但 `apps/desktop/src/main/ipc.ts` 的 `readSession` 处理器是：

```ts
const out = [];
for await (const e of ...read(...)) out.push(e);
return out;          // ← 整个数组一次性过 IPC
```

存储层的克制在边界上被完整抵消。实测 5 万条事件：堆增长 41.9 MB，`structuredClone` 一次 685.6 ms（Electron 的 IPC 编码同源）——主进程和渲染进程各卡一次。

**2026-08-09 已修复**：与 G4 同一次解决，但比原来设想的"快照 + 尾部事件"更进一步——`readSession` 现在直接返回主进程已经 `reduce()` 过的当前状态（`getSessionState()`），渲染层不再自己重放任何历史事件，只对后续的实时事件继续增量 `reduce()`（ADR-0015"内核能在浏览器里跑"这条价值在这部分不受影响）。本地基准：n=50000 的"IPC 物化+渲染层归约"总耗时从 22889ms 降到 227ms（100.9×）。移除了从未被消费过的 `ReadSessionRequest.fromSeq` 字段。

### ~~G6. 两条各自正确的约束合起来，让流式输出渲染不出来~~ ✅ 已做（2026-08-05）→ [ADR-0021](./adr/0021-流式渲染与第二份状态的边界.md)

原倾向（与 `SessionState` 分离的 live buffer）被采纳，并补上了它最需要的那一半：**一条可执行的判据**——*任何时刻，同一段文字要么在 buffer 里，要么在 `state.messages` 里，不会同时在两边*。归零时机（`message.end` / `message.interrupted` / `turn.end` / 新 `message.start`）是全部要害。

实现放在 `packages/kernel/src/state/live-buffer.ts`（纯函数），不是渲染层——CLI（M4）要渲染同一件事，两份实现的归零时机会分叉。

~~**遗留**：停止按钮还没有；`tool.progress` 同样是瞬态事件，"工具正在跑第几步"目前也显示不出来。~~
✅ 两项都已补上：停止按钮在 M1-b（[ADR-0022](./adr/0022-Provider传输层与密钥归宿.md)），
`tool.progress` 在 M1-c 按同一套判据扩展了 `LiveBuffer`。值得记一笔的是两者**满足判据的方式不同**：
delta 的文字最终会原样进 `message.end`，所以它是"先在 buffer、后在 state"；
而 progress 的内容**永远不会进 state**（持久流里对应的是 `tool.end` 的结果），
所以它的判据是"随 `tool.end` 消失"，不是"被取代"。照抄上一半的归零时机会把它挂在
`message.end` 上——那样进度会在模型说完话的那一刻消失，而工具那时才刚开始跑。

## H. M3 微内核化重构留下的未决项（2026-08-14 新增）

十份决策（[ADR-0052 ~ 0061](./adr/README.md)）定案后，刻意留下四个问题不决。

### H1 三方插件的隔离立场

`docs/05 §2.2` 承诺"一插件一进程 + 能力网关 RPC"；参考实现 deepseek-harness 的插件就是
npm 包，全部进程内受信。两者的取舍：

- **进程外网关**：能兑现"插件尝试读 `~/.ssh/id_rsa` 被网关拒绝""插件死循环 5s 后被终止、
  主循环不受影响"这两条验收，但要写两套宿主。
- **全进程内**：实现量小得多、扩展体验最好，但插件崩溃会拖垮主循环，
  且上面两条验收只能靠闸门而非隔离来满足。

**倾向**：分级——一方插件与小明自己生成的插件走进程内受信容器；三方插件与 MCP server 走
进程外网关。**但本次不定案**，与 MCP 隔离一起在 M4 决议。M3 只做进程内容器。

### H2 Code Mode 的语言与沙箱选型

M3-h 只做 TypeScript + Node worker。待定：要不要 Python 运行时；worker 之外要不要真正的
进程/容器隔离（当前立场是 worker 只是稳定性手段，安全由闸门提供，见 ADR-0061）。
**触发重估的条件**：出现一类程序，其失败模式是 worker 拦不住的（例如耗尽主进程句柄）。

### H3 是否需要目标域（goal）与外层循环

参考实现有一个"同会话目标"域：一个持久的完成目标 + 轮次上限 + `active/paused/blocked/complete`
状态机，由一个驱动器在回合结束时决定要不要再来一轮；以及 Ralph 式"每轮开一个全新子会话、
只靠工作区和一份交接报告传递状态"的循环。

小明 M3-d 的 `turn/stopping` 扩展点足以承载这类策略，**但没有实现任何一个**。
未决的是：真的需要一个持久目标域吗，还是"用户再说一句"就够？

**倾向**：等 Inbox 落地后用真实任务试；不要在没有真实需求时先造一个状态机
（ADR-0002 否决双 Agent 状态机是同一个理由）。

### H4 子 Agent 作用域是否两级扁平

参考实现的作用域是**两级扁平**：作用域注册不向子 Agent 继承，子树行为用"血缘"数据表达
而不是作用域结构。小明当前的子 Agent（ADR-0049）是手工过滤父工具集。
M3-a 的容器提供 `ctx.fork()` 之后，是照搬两级扁平，还是允许多级继承？

**倾向**：两级扁平。多级继承会让"这个子 Agent 到底能用哪些工具"变成一个需要沿链求解的问题，
而那正是权限判定最不该有的形状。**待 M3-d 实现时定案并补 ADR。**

---

## 讨论优先级（2026-08-14 更新）

| 优先级 | 问题 | 为什么现在要定 |
|---|---|---|
| 🔴 M3 中 | **H4 子 Agent 作用域是否两级扁平** | M3-a 的 `ctx.fork()` 一落地就必须选一个，改的成本随子 Agent 用法增加而上升 |
| 🔴 M4 前 | **H1 三方插件隔离立场** | 决定 `docs/05 §2.2` 是兑现还是改写；也决定 MCP 怎么接 |
| 🔴 M4 前 | **G2 MCP 污点来源与回传** | 子 Agent 已由 ADR-0049 闭环；MCP 注册仍失败关闭，接入前必须完成真实载体反向演练 |
| 🔴 M4 前 | **C2 受限执行器 / OS 沙箱边界** | 并行、三方代码、插件进程与无人值守不能建立在当前主机直跑边界上。M3-e 的 `ctx.executor` 接缝是它的落点，但只交付 local 提供者 |
| 🟡 M3-h | **H2 Code Mode 语言与沙箱** | 只影响最后一段，可随该段一起定 |
| 🟡 M4 | **C9 用户是否可从 UI 编辑权限规则** | 只读安全面板已完成；若开放写入口，必须保证只有人能放宽且配置路径受自改红线保护 |
| 🟡 M5 之后 | **H3 目标域与外层循环** | 等 Inbox 落地后用真实任务试，不要先造状态机 |
| 🟡 M5–M7 | **D1 会话加密、F1 L4 接受度** | 分别影响本地数据保护与自修改合并边界 |
| 🟢 后续 | **C8 PTY 完整输出/交互、D2 多设备同步、F2 评测集来源** | 不阻塞 M3/M4 首段，但开工前需各自 ADR |
| ✅ 已定并实现 | D3 索引、B3 上下文、G2 子 Agent、M2 编辑/Git/checkpoint | ADR-0043 至 ADR-0049 |
| ✅ 已定未实现 | 插件容器、扩展点边界、能力接缝、Inbox、扩展事件、渲染意图、profile、不变量、Code Mode | ADR-0052 至 ADR-0061（M3 规划） |

**M2 已完成**（含两项人工验收，留痕见 [M2 收官记录](./experience/m2/收官记录.md)）。
当前开放问题主要是 M3 的作用域形状与 M4 的三方扩展隔离边界；不得从上面的历史记录恢复
`ask`、权限档位、会话授权或原始 PTY stdin——**参考实现 deepseek-harness 有 `ask` 与审批接缝，
这不构成把它们搬回来的理由**（ADR-0053 §三）。

## ✅ 工具链冒烟验证已完成（2026-08-04）

开工前的两项外部不确定性均已实机验证，结论写入 [ADR-0010](./adr/0010-TypeScript双编译器工具链.md) 与 [10-契约设计 §11](./10-契约设计.md)。

| 验证项 | 结论 |
|---|---|
| TS 7.0 是否发布 | ✅ `typescript@latest` = **7.0.2** |
| `tsc --noEmit` / d.ts / `--build` 项目引用 / `--watch` / pnpm workspace | ✅ 全部可用 |
| TS 7.0 的 JS 编译器 API | ❌ **完全不提供**，typescript-eslint 硬失败 |
| 对策 | ✅ 官方双装机制 `@typescript/typescript6`，退路 1 已落地 |
| 实测提速 | 2.1x（非"数量级"）；CI 瓶颈是走 TS 6 的 typed lint |
| Zod 4 `z.toJSONSchema()` | ✅ 可用，**但必须传 `io:'input'`** |
| 事件 payload 保留未知字段 | ❌→✅ 必须用 `z.looseObject()`，`z.object()` 会静默丢弃 |

**冒烟额外抓到的两个真实设计缺陷**（已在文档中修正）：泛型 `newId<T>()` 是伪装的类型断言；`z.object()` 无法满足 ADR-0008 的宽松保留要求。

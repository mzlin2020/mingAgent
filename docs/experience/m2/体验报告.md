# M2 体验与整体验收报告

- **结论**：通过
- **日期**：2026-08-13

## 结果

M2-a 至 M2-i 均按顺序完成，每一阶段均先通过聚焦测试和全量闸门再进入下一阶段。最终独立总验收：

- 100 个测试文件：99 passed，1 skipped。
- 1124 项测试：1103 passed，21 skipped。
- 内核覆盖测试 669 项全部通过；行覆盖率 96.8%，分支覆盖率 86.06%。
- dist headless smoke 同次贯通 M2-b 至 M2-i；M2-a 的 todo 也在主 DoD 回合中执行。
- SIGKILL 写租约恢复、依赖边界、供应链检查、size-limit 全部通过。
- Electron renderer/main/preload 三段生产构建成功，renderer 共转换 444 个模块。

## 关键反向证据

- 多文件内容漂移时零文件写入；中途 I/O 失败由同组 checkpoint 收敛恢复。
- path-only commit 未夹带用户既有暂存；hook、冲突、非仓库和中断均返回不同结构化错误。
- 索引冷/损坏/短查询不阻断搜索，自动回到 ripgrep。
- 近期原文本身超过硬预算时不向 Provider 发送已知超限请求。
- 子 Agent 手工请求 `edit.apply` 时分发失败，执行计数保持 0；子 Agent 读网页后父会话实际收到
  `net.fetch` 粘性污点。删除合并逻辑会使该真实派生用例转红。

## 环境记录

`electron-builder --dir` 在本机解压 Electron 运行时后，Windows 对
`win-unpacked.tmp/resources/default_app.asar` 持有外部文件锁，导致临时目录重命名报 `EPERM`；
换全新输出目录仍相同。失败发生在应用文件打包之前，桌面三段生产构建和仓库定义的 `pnpm verify`
均成功。该环境锁不计为 M2 产品回归，但不声称本轮产出了可分发安装目录。

# M3 实施前基线记录

**日期**：2026-08-14
**代码基点**：`2dc0bad`（`main`，与 `origin/main` 一致）
**结论**：M0–M2 的代码、安全底座与自动化闸门基线全部通过，可以进入 M3-a。

## 1. 现场收敛

- 开始时工作区有一处未完成的 UI 改动：删除 `UntrustedBanner` 及其挂载点。
  该改动会移除 ADR-0019 / ADR-0039 规定的不可信上下文解除入口，因此没有纳入 M3 基线；
  恢复后两个渲染文件的内容哈希均与 `HEAD` 一致。
- 参考实现 deepseek-harness 的本机源码路径确认为
  `C:\Users\EDY\Desktop\code_mine\deepseek-harness`。M3 可以借鉴其容器与扩展点机制，
  但必须先按 `docs/11-微内核与插件容器.md` §4 排除 `ask`、无特权内核和全进程内受信等
  与小明现有边界冲突的设计。
- pnpm 首次运行时发现现有 `node_modules` 与 store 配置不一致；以 CI 非交互模式重建依赖后，
  锁文件未发生变化，完整 `pnpm verify` 通过。

## 2. 自动化证据

执行入口：PowerShell 中设置 `$env:CI='true'` 后运行 `pnpm verify`。

| 闸门 | 结果 |
|---|---|
| 双编译器工具链 | 原生 `tsc` 7.0.2；TypeScript JS API 6.0.2 |
| 工作流 / tsconfig paths / 文件规模 / 本地供应链 | 全部通过；176 个生产文件满足 400 行纪律或已有登记豁免 |
| 类型检查 | `tsc -b`、测试、desktop main、desktop renderer 四段全部通过 |
| ESLint | 通过 |
| 全量测试 | 102 个测试文件：101 通过、1 跳过；1161 项：1138 通过、23 跳过 |
| Kernel 覆盖率 | 语句 93.54%、分支 86.06%、函数 97.61%、行 96.8% |
| Headless smoke | 通过；165 条持久事件、231 条总线事件，M2-b～M2-i 与安全红线链路贯通 |
| 写租约崩溃恢复 | 通过；死亡 PID 租约回收与孤儿会话识别正确 |
| 依赖边界 | 316 个模块、1267 条依赖，无违规 |
| size-limit | 8.14 kB / 15 kB，通过 |

## 3. 进入 M3-a 的边界

- M3 的运行时代码仍未开工；本轮只收敛基线与项目记忆。
- M3-a 开工前先定案 H4：`ctx.fork()` 的子 Agent 作用域继承语义，并新增 ADR。
- M3-a 只实现 `@xm/kernel/src/container/`、确定性时钟与 ID 服务及其测试，
  不接入现有业务代码，不改变现有事件流与用户行为。
- 阶段结束时必须再次运行同一套 `pnpm verify`，并与本记录对比。
